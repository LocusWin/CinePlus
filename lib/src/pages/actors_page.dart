import 'package:flutter/material.dart';

class ActorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Actors Page'),
      ),
    );
  }
}
